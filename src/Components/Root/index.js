import React, { Fragment } from 'react';
import GlobalStyle from 'styles/global';
import App from 'Components/App';

const Root = () => (
  <Fragment>
    <GlobalStyle />
    <App />
  </Fragment>
);

export default Root;
